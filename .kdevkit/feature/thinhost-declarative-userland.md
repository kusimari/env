# Feature: thinhost-declarative-userland

## Git Setup

- Branch: feat/thinhost-declarative-userland
- Base: main

## Feature Brief

Establish a "Thin Host + Declarative Userland" architecture for `ubuntu-mane`. Reduce the Ubuntu host OS to a minimal hardware/kernel/display appliance, manage all primary user-facing desktop applications (file manager, archive tool, image viewer, browser, media, editor) declaratively via Nix, establish declarative XDG default MIME associations, and provide a dedicated, gracefully degrading host debloat sub-script.

## Handoff

Phase: planning
Ready for: user review of spec revisions
Carry forward: public repo hygiene — no internal markers

## Requirements

- **Nix Declarative Userland**:
  - `pkgs.xfce.thunar` and `pkgs.xfce.thunar-archive-plugin` installed for GUI file management.
  - `pkgs.xarchiver` installed as the archive compression and extraction backend.
  - `pkgs.viewnior` installed as a lightweight standalone image viewer for quick file-opening outside DigiKam.
  - Declarative `xdg.mimeApps` default associations configured in Home Manager for `ubuntu-mane`:
    - `inode/directory` -> `thunar.desktop`
    - `image/png`, `image/jpeg`, `image/webp`, `image/gif` -> `viewnior.desktop`
    - `application/zip`, `application/x-tar`, `application/x-gzip`, `application/x-bzip2`, `application/x-xz` -> `xarchiver.desktop`
  - All Nix desktop applications are indexed and searchable via Rofi (`rofi -show drun`).
- **Thin Host OS Debloat Sub-Script (`layers/layer-1-debloat-ubuntu-mane.sh`)**:
  - Provide a standalone sub-script dedicated to purging default Ubuntu desktop bloatware (LibreOffice, Thunderbird, Rhythmbox, Transmission, Totem, GNOME games, Nautilus).
  - Graceful degradation: test package presence (`dpkg -s`) before removal; missing packages are silently skipped rather than failing, ensuring compatibility across Ubuntu releases and minimal installs.
  - Extensibility: maintain bloat targets as an explicit array so future Ubuntu-introduced bloatware can be added easily.
  - Safety: honor `--dry-run` to preview packages slated for removal before making mutations.
- **Verification & Compatibility**:
  - Headless kelasa environments (`al2-kelasa`, `al2023-kelasa`) and darwin configurations remain unaffected.
  - `layers/test-flake.sh` passes cleanly.

## Test Strategy

- `layers/test-flake.sh` builds both `ubuntu-mane` and `al2-kelasa` configurations to verify flake integrity.
- Verify `thunar`, `xarchiver`, and `viewnior` binaries on PATH and `.desktop` files in `~/.nix-profile/share/applications/`.
- Verify `~/.config/mimeapps.list` generated by Home Manager sets Thunar, Viewnior, and Xarchiver as defaults.
- Verify Rofi search displays new apps.
- Test `layers/layer-1-debloat-ubuntu-mane.sh --dry-run` to ensure safe discovery and graceful degradation on absent packages.

## Design

- **Architecture**: Clean separation of concerns:
  - Host OS (Ubuntu / APT): Linux kernel, GPU/Mesa/Vulkan drivers, display server (X11), desktop shell (GNOME), systemd.
  - Userland (Nix Flake): All applications, utilities, launchers, MIME associations, and tools.
- **Packaging (`envKinds/mane/ubuntu.nix`)**:
  - Add `pkgs.xfce.thunar`, `pkgs.xfce.thunar-archive-plugin`, `pkgs.xarchiver`, and `pkgs.viewnior` to `home.packages`.
  - Add `xdg.mimeApps = { enable = true; defaultApplications = { ... }; };`.
- **Debloat Sub-Script (`layers/layer-1-debloat-ubuntu-mane.sh`)**:
  - Dedicated executable script invoked independently or chained from Layer 1.
  - Uses an extensible bash array of known default Ubuntu desktop bloat packages.
  - Probes installed status via `dpkg -s` so only present packages are targeted.
  - Purges identified bloat packages and runs autoremove for orphaned dependencies.

## Implementation Plan

- [ ] Add `pkgs.xfce.thunar`, `pkgs.xfce.thunar-archive-plugin`, `pkgs.xarchiver`, and `pkgs.viewnior` to `envKinds/mane/ubuntu.nix`
- [ ] Configure `xdg.mimeApps` default associations in `envKinds/mane/ubuntu.nix`
- [ ] Create `layers/layer-1-debloat-ubuntu-mane.sh` with presence check (`dpkg -s`), `--dry-run`, and extensible bloat list
- [ ] Run `bash layers/test-flake.sh` to verify build integrity for all targets
- [ ] Rebuild Home Manager generation via `bash layers/layer-3-ubuntu-mane.sh`
- [ ] Verify Rofi indexing, Thunar launching, and MIME defaults
- [ ] Execute `bash layers/layer-1-debloat-ubuntu-mane.sh` to purge redundant APT packages

## Decision Log

- Dropped "Host Preserved Exceptions" section (Steam, GNOME System Monitor) per PR feedback — neither are part of default Ubuntu bloatware, and planning for untouched apps adds unnecessary noise.
- Structured host debloat as a dedicated sub-script (`layers/layer-1-debloat-ubuntu-mane.sh`) with dynamic `dpkg -s` probing to gracefully degrade across Ubuntu releases and allow simple addition of future bloat targets.

## Session Log

- 2026-09-13: Pivoted scope from `thunar-file-manager` to `thinhost-declarative-userland`. Full spec authored.
- 2026-09-13: Addressed PR review feedback on PR #52. Removed redundant preserved exceptions, established `layers/layer-1-debloat-ubuntu-mane.sh` sub-script design with graceful degradation and extensible package array.
