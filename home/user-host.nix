# WARNING: This file must contain placeholder values in git
# The build script (build-nix/_common.sh) temporarily replaces these
# values during build and restores them afterwards. Never commit
# actual username or hostname values.
{
  user = "replace-user";
  hostName = "replace-hostname";
}
